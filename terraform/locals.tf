locals {
  sdfc_credentials = {
    consumer_key    = var.sfdc_consumer_key
    consumer_secret = var.sfdc_consumer_secret
    access_token    = var.sfdc_access_token
    password        = var.sfdc_password
  }
}
